#include <iostream>
#include <vector>
#include <unordered_map>

bool canSplit(vector<int> nums)
{
	int length = nums.size();
	if(length < 7) return false;

	vector<int> sum;
	unordered_map<int, vector<int>& > posMap;
	
	sum.push_back(nums[0]);
	vector<int> pos;
	pos.push_back(0);
	posMap.emplace(nums[0], pos)

	for(auto n : nums)
	{

		
	}

}
