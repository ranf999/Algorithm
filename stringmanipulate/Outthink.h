#include <iostream>
#include <string>
#include <vector>

using namespace std;

class solution1
{
public:
	void printLine(int num, int p, int q);
	bool divisible(int i, int p, int q);
	bool contain(int i, int p, int q);
};
